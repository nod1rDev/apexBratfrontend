// features/counterSlice.js
import { createSlice } from '@reduxjs/toolkit';

const controlSlice = createSlice({
  name: 'controlSlice',
  initialState: {
    value: 0,
  },
  reducers: {
   
  },
});

export const {  } = controlSlice.actions;
export default controlSlice.reducer;
